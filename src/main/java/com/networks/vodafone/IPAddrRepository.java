package com.networks.vodafone;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * Standard repository for IPAddr objects.
 */
interface IPAddrRepository extends JpaRepository<IPAddr, Long> {

}
